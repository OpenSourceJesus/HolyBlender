import clr

#💠rotateSpeed: 50.0
class Rotate(MonoBehaviour):
    def __init__(self):
        self.rotateSpeed = 50.0
        super(Rotate, self).__init__()
    def Update(self):
        transform.eulerAngles += Vector3.up * rotateSpeed * Time.deltaTime
rotateSpeed  : float= 50.0
